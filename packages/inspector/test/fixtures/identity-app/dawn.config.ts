// Discoverable Dawn app whose /notes route declares CUSTOM identity keys —
// proves the inspector's approve path resolves and uses them (identityFallback
// false), mirroring the CLI's resolveIdentityKeys.
export default {
  appDir: "src/app",
}
