{
  "files": {
    "src/app/(public)/hello/[tenant]/index.ts": "import type { HelloState } from \"./state.js\";\n\nexport const graph = async (state: HelloState): Promise<HelloState> => ({\n  ...state,\n  greeting: `Hello, ${state.tenant}!`,\n});\n"
  },
  "input": {
    "tenant": "graph-tenant"
  },
  "routeFile": "src/app/(public)/hello/[tenant]/index.ts",
  "expected": {
    "mode": "graph",
    "output": {
      "greeting": "Hello, graph-tenant!",
      "tenant": "graph-tenant"
    },
    "status": "passed"
  }
}
